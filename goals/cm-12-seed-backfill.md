---
task: cm-12
spec: docs/plans/2026-06-27-commerce-tenant-db-migration-plan.md
depends_on: [cm-11]
verify: pnpm exec tsc --noEmit && pnpm lint && pnpm test && pnpm build
verify_fix_cap: 3
verify_timeout_s: 2400
---

# Goal

Implement spec **CM-12** (section "### CM-12 — Seed rewrite + demo tenant backfill" of `docs/plans/2026-06-27-commerce-tenant-db-migration-plan.md`, plus §5.5 + §9), as part of the **EXPAND-CONTRACT migration Marlin decided 2026-06-27**. FLIP `seedDemoSite.ts` so ALL commerce writes (including the 3 ex-direct `prisma.stockLocation/inventoryItem/inventoryLevel.create` calls) route through the scoped `commerceTenantDb(tgId)` handle via the NEW Kysely repo funcs, and add a one-shot backfill that provisions the existing demo tenant-group's `tg_<demo>` schema and copies the current `commerce` rows into it, so the demo storefront renders + checks out identically on the new path.

**Read this framing first:** the seed is a LEAF entry point: nothing else in the program imports its internals, so flipping it from the old Prisma path to the NEW Kysely path here does NOT break any other spec's whole-program typecheck. That is why the seed flips in CM-12 (not CM-13): by now CM-06's NEW Kysely `catalogRepositoryKysely` / `pricingRepositoryKysely` exist and CM-11 can provision `tg_<demo>`. Flipping the seed here removes the LAST importer of `withTenant` (the orders route was already flipped in CM-10), which is exactly what UNBLOCKS CM-13 to delete `withTenant.ts`. You do NOT delete `withTenant.ts` yourself (CM-13 owns that) and you do NOT drop the old `commerce` schema (CM-13, after the demo verifies green): the backfill is purely additive, both schemas coexist until then so a deploy rollback re-points to the old path.

## Read first (the seed + the backfill contract)

- The plan's CM-12 section + §5.5 ("Seed rewrite": ALL writes through the scoped handle, the 3 direct creates become schema-qualified inserts, no write bypasses the handle) + §9 (the back-compat cutover: provision `tg_<demo>`, backfill `INSERT INTO tg_demo.<t> SELECT * FROM commerce.<t>` per table respecting FK order, do NOT copy the GENERATED/trigger columns, let them regenerate; the old `commerce` schema stays until CM-13).
- The source to flip: `src/lib/renderer/server/seedDemoSite.ts` (it uses `withTenant(prisma, (tx) => catalogRepository.*(tx, ...))` and `pricingRepository.*` for catalog/pricing, PLUS 3 DIRECT `prisma.stockLocation.create` / `prisma.inventoryItem.create` / `prisma.inventoryLevel.create` at ~:156-166 that ran WITHOUT `withTenant`, PLUS the non-commerce `prisma.site/sitePage/siteDomain.create` calls which are CMS, NOT commerce, and stay on Prisma).
- The NEW funcs the seed now calls: CM-06's `catalogRepositoryKysely` / `pricingRepositoryKysely` (the `(db, args)` Kysely path, still carrying their temporary `Kysely` suffix). The base + provisioning: CM-02's `src/server/commerce/db.ts` (`getCommerceBase`/`commerceTenantDb`), CM-11's `src/server/commerce/provisioning/provision.ts` (`provisionCommerceTenant`). The owner-connection backfill pattern: CM-03's `provisioning/public.ts` (direct owner postgres.js connection from `COMMERCE_OWNER_DATABASE_URL`).
- The demo tenant-group id source: how the demo site's `tenantGroupId` is resolved (the same field `PublishedSite.tenantGroupId` / `scope.ts` exposes). The backfill needs the demo tenant-group id to derive `tg_<demo>` and to scope the seed.

## Definition of done (FLIP the leaf seed + add the backfill; delete nothing)

- `seedDemoSite.ts` commerce writes flipped: derive the demo `tgId` and `const db = commerceTenantDb(tgId)`; route EVERY commerce write through `db` via the NEW Kysely funcs (catalog via `catalogRepositoryKysely`, pricing via `pricingRepositoryKysely`, and the 3 ex-direct creates become `db.insertInto('stock_location'|'inventory_item'|'inventory_level').values(...)`). NO commerce write bypasses the scoped handle. The CMS writes (`site` / `sitePage` / `siteDomain`) stay on Prisma (they are not part of this migration). REMOVE the seed's `withTenant`, `catalogRepository`, `pricingRepository` (old Prisma) imports: this seed is the last `withTenant` importer, and removing it is what lets CM-13 delete `withTenant.ts`.
- New `src/server/commerce/provisioning/backfill-demo.ts`: a one-shot `backfillDemoTenant(opts: { tenantGroupId: string; connectionString?: string }): Promise<void>` that, on the OWNER connection: (1) ensures `tg_<demo>` is provisioned (call `provisionCommerceTenant` or assert it exists), (2) copies the current `commerce.<table>` rows into `tg_<demo>.<table>` per table in FK-safe order (inventory_item, stock_location, inventory_level, stock_movement, reservation, product, product_option, product_option_value, product_variant, product_variant_option, price_set, price, price_rule, price_list, credit_note, credit_note_ref, "order", order_line_item, fulfillment_location_default), EXPLICITLY listing columns so the GENERATED `available_quantity` and the trigger-maintained `option_signature` are NOT copied (they regenerate). Idempotent / re-runnable (guard against double-insert, e.g. `ON CONFLICT DO NOTHING` on the natural keys or a pre-check that the target schema is empty). Runs as the owner role.
- A `pnpm db:backfill-demo` script (a tsx entry) invoking `backfillDemoTenant` with the demo tenant-group id from env/config; operator/deploy step, expects `COMMERCE_OWNER_DATABASE_URL`. Do NOT hardcode connection strings or the demo id literal if it is available from config.
- Test (`.itest.ts`, Testcontainer, TRUST AUTH): seed into `tg_<demo>` runs ENTIRELY through `commerceTenantDb` (assert no write hit `public` / the old `commerce` schema); the backfill copies a representative `commerce` dataset into `tg_<demo>` and the GENERATED/trigger columns recompute correctly in the target (available_quantity = stocked - reserved; option_signature populated); the demo read path (`getCommerceServerRepositoryDb(commerceTenantDb(tgDemo))`) returns the seeded catalog, and a place-order against `tg_<demo>` (via the NEW `createOrderKysely`) succeeds. Re-running the backfill is a no-op.
- `pnpm exec tsc --noEmit && pnpm lint && pnpm test && pnpm build` pass. Single conventional commit e.g. `feat(commerce): flip seed through scoped handle + demo tenant backfill into tg_<demo> (CM-12)`.

## Constraints

- Stay in this worktree. Files: `src/lib/renderer/server/seedDemoSite.ts` (commerce writes only), new `src/server/commerce/provisioning/backfill-demo.ts`, the `db:backfill-demo` script (+ tsx entry), the `.itest.ts`, and the `package.json` script line. Do NOT touch the repos/routes (CM-06..CM-10), the migration set (CM-04), provisioning/provision.ts (CM-11, consumed), or `prisma/schema.prisma` (CM-13).
- Do NOT delete `withTenant.ts` (CM-13 owns it, now safely deletable because this spec removed the seed's import). Do NOT drop the old `commerce` schema (CM-13 owns that, ONLY after the demo verifies green on `tg_<demo>`). The backfill is additive: both schemas coexist until CM-13, so a deploy rollback re-points to the old constant path.
- Backfill runs as the OWNER role (it does cross-schema INSERT...SELECT and writes append-only tables the app role cannot). Do NOT copy `available_quantity` or `option_signature` (they are GENERATED / trigger-maintained; copying them would either error or desync). Respect FK order so a child never inserts before its parent.
- Preserve the decided model: `tg_<hex32>` schema derived from the demo tenantGroupId via the runner's chokepoint (do NOT hand-build the schema name). No `SET LOCAL search_path` in app code.
- Testcontainer tests use the container's TRUST auth (`POSTGRES_HOST_AUTH_METHOD: 'trust'` + username-only `postgresql://role@host` URIs, NO password literals: hardcoded passwords trip GitGuardian and block the PR).
- Do not push to any remote. Output a final completion message confirming: the seed routes every commerce write through `commerceTenantDb` (and no longer imports `withTenant`), the backfill column-list excludes the GENERATED/trigger columns, and the demo renders + checks out on `tg_<demo>`.

## Notes

- §9 is the back-compat spine: the existing demo is the one current commerce tenant; this backfill is a single schema copy (no fan-out risk pre-MVP). SEQUENCING (operator reorder 2026-06-27): the tail runs CM-11 (done) -> CM-12 (this) -> CM-10 (route/render flip) -> CM-13, i.e. BACKFILL BEFORE FLIP. So CM-10 is NOT merged yet when you run this; that is fine, this spec does not need it: the seed writes via `commerceTenantDb` directly and the test calls the NEW funcs (`getCommerceServerRepositoryDb` / `createOrderKysely`) DIRECTLY, not through the routes. CM-10 then flips the live routes onto the `tg_<demo>` this spec populated; CM-13 drops the old `commerce` schema only after the demo verifies green on `tg_<demo>`.
- The 3 ex-direct `prisma.*.create` calls in the seed were the only commerce writes that bypassed `withTenant` (they relied on Prisma `@@schema` qualification). After this flip there is NO bypass: assert it in the test (a write to `public`/`commerce` instead of `tg_<demo>` is a failure).
- The NEW funcs the seed now calls still carry their temporary `*Kysely` suffix. CM-13 renames them to canonical and updates this seed's caller lines. Do not pre-rename.
- The `.itest.ts` runs under CI (Docker), excluded from the in-loop unit `pnpm test`. The in-loop verify is unit + build; the seed-through-handle + backfill-recompute proofs are the CI gate, and the live demo render + checkout on `tg_<demo>` is the operator's gate before CM-13's destructive teardown.
