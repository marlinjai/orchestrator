---
task: cm-13
spec: docs/plans/2026-06-27-commerce-tenant-db-migration-plan.md
depends_on: [cm-10, cm-11, cm-12]
shared_state: [prisma]
verify: pnpm exec tsc --noEmit && pnpm lint && pnpm test && pnpm build
verify_fix_cap: 3
verify_timeout_s: 2400
---

# Goal

Implement spec **CM-13** (section "### CM-13 — CI guards + Prisma teardown" of `docs/plans/2026-06-27-commerce-tenant-db-migration-plan.md`, plus §6), as the **CONTRACT step of the EXPAND-CONTRACT strategy Marlin decided 2026-06-27**. This is the final teardown: it DELETES the old Prisma path (now that nothing calls it), RENAMES the temporary-suffixed Kysely funcs to canonical, wires the `check-guards.mjs` cross-tenant-leak lint, removes the commerce models from `prisma/schema.prisma`, and lands the consolidated crown-jewel isolation spec. It runs ONLY after the demo is verified green on `tg_<demo>` (CM-12).

**Read this framing first, it is the whole job:** CM-06..CM-09 EXPANDED (added new Kysely funcs alongside the old Prisma ones, with temporary `*Kysely` / `getCommerceServerRepositoryDb` / `createOrderKysely` suffixes). CM-10 flipped the routes + render, CM-12 flipped the seed, both to the new funcs. So the OLD Prisma path now has ZERO callers (except possibly old unit tests you delete with it). This spec CONTRACTS: (1) delete the old Prisma repo objects/funcs + `withTenant.ts` + `resolveCommerceSchemaForSite` + the adapter-prisma commerce usage + the old `@prisma/client`-typed interfaces + the 19 commerce models/9 enums from `prisma/schema.prisma`; (2) RENAME the temp-suffixed new funcs to the canonical names the old ones had, and update the CM-10/CM-12 caller lines (routes, `page.tsx`, seed) + the barrel; (3) wire the guards + the crown-jewel test. After this, the commerce module is 100% Kysely/tenant-db with canonical names and no Prisma.

## Read first (the teardown surface + the guard source)

- The plan's CM-13 section + §6 (the crown-jewel isolation test + `check-guards.mjs` over `src/server/commerce/**` with the extended `KNOWN_TENANT_TABLES`, listing all 19 commerce tables).
- The guard source to vendor/configure: `auth-brain/packages/tenant-db/scripts/check-guards.mjs` (the G1-G5 guards: no bare known-tenant table in `sql\`\``, no `.withSchema(` outside tenant-db, no runtime `SET search_path` in app code) and `auth-brain/packages/tenant-db/tests/check-guards.spec.ts`. Adapt `KNOWN_TENANT_TABLES` to the 19 commerce tables: `product`, `product_option`, `product_option_value`, `product_variant`, `product_variant_option`, `price_set`, `price`, `price_rule`, `price_list`, `credit_note`, `credit_note_ref`, `inventory_item`, `stock_location`, `inventory_level`, `stock_movement`, `reservation`, `fulfillment_location_default`, `order`, `order_line_item`.
- The OLD path to DELETE (now caller-less): the old Prisma objects `catalogRepository`/`pricingRepository` (`repository/catalog.ts`, `pricing.ts`), `commerceReadRepository` + old `getCommerceServerRepository(prisma?, schema?)` (`repository/read.ts`), `orderRepository` (`repository/order.ts`), old `createOrder`/`runOrderTransaction`/`resolvePriorOrder` + old `isOrderRequestIdConflict` (`order/createOrder.ts`), the old Prisma reserve funcs `reserve`/`reserveKit`/`applyInventoryEffect`/`*WithRetry`/`reserveTransaction` + old `isRequestIdUniqueViolation` (`inventory/reserve.ts`), `resolveCommerceSchemaForSite` + its `COMMERCE_SCHEMA` import (`tenant.ts`), `src/server/commerce/withTenant.ts` (DELETE the file), the barrel's `export { COMMERCE_SCHEMA, withTenant }` line, and the old `@prisma/client`-typed interfaces `CatalogRepository`/`PricingRepository`/`OrderRepository`/`InventoryRepository` + the commerce row-type imports in `repository/types.ts`.
- The temp-suffixed NEW funcs to RENAME to canonical + their callers: `catalogRepositoryKysely`->`catalogRepository`, `pricingRepositoryKysely`->`pricingRepository`, `commerceReadRepositoryKysely`->`commerceReadRepository`, `getCommerceServerRepositoryDb`->`getCommerceServerRepository`, `orderRepositoryKysely`->`orderRepository`, `createOrderKysely`->`createOrder`, `reserveKysely`->`reserve` (+ `reserveKit`/`applyInventoryEffect`/`*WithRetry`), `isOrderRequestIdConflictPg`->`isOrderRequestIdConflict`, `isRequestIdUniqueViolationPg`->`isRequestIdUniqueViolation`. Callers to update: the 4 routes + `page.tsx` (CM-10), `seedDemoSite.ts` (CM-12), the barrel `src/server/commerce/index.ts`, and any new co-located interface names.
- The Prisma teardown surface: `prisma/schema.prisma` (the 19 `@@schema("commerce")` models + the 9 commerce enums to remove; drop `"commerce"` from the `schemas` array + the multiSchema preview if nothing else uses it). `src/server/db.ts` (the `getPrismaClient()` singleton stays for CMS; remove any commerce-only surface). The framer-clone CI workflow (`.github/workflows/*`) to add the guard step.

## Definition of done (CONTRACT: delete old, rename new to canonical, wire guards)

- DELETE the old Prisma path enumerated above: the old repo objects/funcs, `withTenant.ts` (file removed), the barrel's `COMMERCE_SCHEMA, withTenant` re-export, `resolveCommerceSchemaForSite`, the old `@prisma/client`-typed interfaces + commerce row-type imports in `repository/types.ts`, and any old unit tests that drove the Prisma path (the Kysely isolation/concurrency specs from CM-06..CM-09 + CM-12 are the replacement coverage). The `adapter-prisma` (data-table Prisma adapter) usage is confirmed gone from the commerce path (it was a verified no-op there).
- RENAME the temp-suffixed new funcs to the canonical names and update every caller (routes, `page.tsx`, seed, the barrel, co-located interfaces). After the rename, `git grep -n "Kysely\|Db\b" src/server/commerce` shows no surviving temp-suffix on a commerce repo/factory/reserve/order symbol, and the barrel exports the canonical `getCommerceServerRepository` (now the scoped-handle one) + `commerceReadRepository`.
- `prisma/schema.prisma`: the 19 commerce models + 9 commerce enums removed; the `commerce` schema reference dropped from the `schemas` array if nothing else references it (keep only what CMS still uses). The remaining schema validates (`prisma validate` / `prisma generate` succeeds) and CMS models are untouched.
- `scripts/check-guards.mjs` (vendored/configured) with `KNOWN_TENANT_TABLES` = the 19 commerce tables, run over `src/server/commerce/**` (and the commerce routes + seed) via a `pnpm lint:guards` script. It enforces: no bare commerce table inside a `sql\`\`` fragment (G3: must use `tenantSchemaRef`), no `.withSchema(` outside `@marlinjai/tenant-db`, no runtime `SET search_path` in app code. The guard PASSES over the post-rename commerce source. The framer-clone CI workflow runs `pnpm lint:guards` as a gating step.
- The consolidated crown-jewel `isolation.spec.ts` (mirroring `auth-brain/packages/tenant-db/src/tests/isolation.spec.ts`, adapted to commerce) exists and passes: TG_A/TG_B provision + the public DECOY (same-named `public.product`/`public.order`/`inventory_level` carrying both tenants' identifiers) + the real (now canonically-named) commerce repo functions scoped to `tg_a` returning ONLY tg_a rows; a `tg_b` handle for a `tg_a` order id returning ZERO (the schema wall); the hazard-proof (a guard-disabled BARE `FROM inventory_level` fragment asserted to LEAK to the decoy, pinning WHY G3 exists); and the GRANT proof (commerce_app scoped to tg_a denied on tg_b). If CM-06..CM-09 already landed per-repo isolation specs, this spec CONSOLIDATES/ensures the full crown-jewel exists (the hazard-proof + grant proof in one place); do NOT duplicate, extend.
- `pnpm exec tsc --noEmit && pnpm lint && pnpm test && pnpm build` pass AND `pnpm lint:guards` passes. Single conventional commit e.g. `feat(commerce): CI tenant-leak guards + Prisma commerce teardown + canonical rename (CM-13)`.

## Constraints

- This is the ONLY destructive teardown spec. It runs after CM-12 verified the demo on `tg_<demo>`. The old `commerce` SCHEMA DROP itself is an operator/DB step (a migration or a manual owner action), gated on the demo verification per §10 rollback; this spec removes the Prisma MODELS + the old code path from the codebase, NOT necessarily the live `commerce` Postgres schema. Note which, and leave the destructive DB drop to the operator with the retained backfill source as the rollback path.
- Files: `scripts/check-guards.mjs` + the `lint:guards` script + the CI workflow edit; `prisma/schema.prisma`; `src/server/db.ts`; the commerce repo files (`catalog.ts`, `pricing.ts`, `read.ts`, `order.ts`, `createOrder.ts`, `reserve.ts`) for the delete-old + rename-new; `tenant.ts` (drop `resolveCommerceSchemaForSite`); `withTenant.ts` (DELETE); the barrel `index.ts`; `repository/types.ts` (drop dead interfaces + commerce `@prisma/client` imports); the 4 routes + `page.tsx` + `seedDemoSite.ts` (rename callers); the crown-jewel `isolation.spec.ts`. Do NOT touch the migration set (CM-04) or provisioning (CM-11).
- Preserve the decided guards: `tenantSchemaRef` for raw SQL, no `SET LOCAL search_path` in app code (migrations only), `.withSchema(` only via `@marlinjai/tenant-db`. The guard's `KNOWN_TENANT_TABLES` MUST cover all 19 commerce tables or a leak slips through.
- After the delete, NO commerce `@prisma/client` import may survive a green `tsc`: the new path was always typed via `CommerceDB` (CM-05). If `tsc` flags a surviving commerce Prisma import you did not expect, that is a real gap in an earlier spec: fix it to use the `CommerceDB`/DTO types here and FLAG which file under-migrated.
- Testcontainer/isolation tests use the container's TRUST auth (`POSTGRES_HOST_AUTH_METHOD: 'trust'` + username-only `postgresql://role@host` URIs, NO password literals: hardcoded passwords trip GitGuardian and block the PR).
- Do not push to any remote. Output a final completion message listing: the `KNOWN_TENANT_TABLES` you configured, what was deleted (old funcs + `withTenant.ts` + Prisma models), the temp->canonical renames you applied + the caller files updated, any surviving commerce Prisma import you had to fix, and confirmation the crown-jewel isolation spec + the guard lint both pass.

## Notes

- V1 PLAN-GAP RESOLVED: the v1 worried that `repository/types.ts` imports commerce row types from `@prisma/client` and that dropping the models breaks them. Under expand-contract the NEW repos never used those interfaces (they carry co-located `CommerceDB`-typed interfaces), so the old `@prisma/client`-typed interfaces are simply DELETED here with the old repos. No row-type migration is needed; just confirm no commerce `@prisma/client` symbol survives `tsc`.
- The canonical RENAME is the no-tech-debt close-out: the temp `*Kysely` / `*Db` suffixes existed only to let the new path coexist with the old during EXPAND/MIGRATE. With the old path deleted, the suffixes are dead weight; rename to canonical so the final source reads as if it had always been Kysely. This is a large but purely mechanical diff (delete + rename + update imports).
- The `shared_state: [prisma]` tag serializes this spec against any other task editing `prisma/schema.prisma` (a safety net for parallel non-commerce work: no other commerce spec edits the schema).
- The crown-jewel `isolation.spec.ts` is the single highest-leverage compliance artifact (SOC2 Type II operating-effectiveness evidence, automated + CI-run). The grant-denied probe proves the wall is enforced by GRANTS, not app discipline. The `.itest`/isolation tests run under CI (Docker); the in-loop verify is unit + build + the guard lint (a pure-static check that DOES run in-loop). Make sure `pnpm lint:guards` is wired into the verify-able commands so the gate actually catches a leak.
