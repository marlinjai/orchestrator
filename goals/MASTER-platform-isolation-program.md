---
type: plan
status: archived
date: 2026-07-27
summary: Master prompt for the Lumitra platform isolation program (org consolidation, storage waves S1-S4, machine APIs). Executed in full 2026-07-27; retained as the historical record of the predecessor program.
tags: [platform-isolation, auth-brain, storage-brain, machine-api]
---

# MASTER PROMPT: Lumitra platform isolation program, end to end

You are the OPERATOR (Claude Code with the `autonomous-orchestration` skill), not a Worker. Marlin authorized this entire program on 2026-07-27 ("you have my go for everything"), INCLUDING the tier-3 auth-brain dispatches named below; cite this authorization when passing `--confirm-stakes`. It does NOT cover new tier-3 work outside this list. Follow the skill's canonical flow for every slice: worktree off fresh origin/main, dispatch, review the diff against the goal, run the repo's FULL verify chain (match its CI exactly, including build where CI builds), push, PR, wait CI, squash-merge, watch the deploy workflow, clean up worktree + branches. Never push to main. All secret-touching ops go through `execute_with_secrets` (values never in context); UUIDs are not secrets. Diff Worker branches against `git merge-base HEAD origin/main`, never raw origin/main. If a Worker adds/changes dependencies, verify `pnpm-lock.yaml` is committed.

Repos: auth-brain `~/software-dev/ERP-suite/projects/lumitra-infra/auth-brain` (tier 3), analytics-platform + lumitra-studio + receipt-ocr-app under `~/software-dev/ERP-suite/projects/` (tier 2), storage-brain has NO local checkout (clone `https://github.com/marlinjai/storage-brain.git`). Goal files live in THIS directory. Infisical project ids: auth-brain `97c4971e-78c1-4adf-83e1-6c0b5f13375c`, studio `934b272e-0a37-4eb1-9b12-0530d3d2ff23`, analytics `45b9c32b-3deb-42ef-ad8a-9a931b86c01a`, infra `6adabd49-59d3-4bab-8a1e-c104a0da3c64`, cc-wrapper `9af620f0-be34-4a82-859b-0026306eea64` (all env prod, path `/` unless stated). Auth-brain admin machine API: `https://auth.lumitra.co/api/admin/machine/*` with `$ADMIN_API_KEY` injected by the proxy from the auth-brain project.

Known ids: Lumitra org (tenant_group) `019ec2f2-f189-77aa-9843-0ac406283e44`. Companies: lumitra-core (SURVIVOR) `019ec2f2-f19e-70f4-a889-8afb34c314ca`, lumitra-analytics `019ee142-440a-7e49-923a-fd0308047670`, lumitra (dying) `019f6a90-8bce-73a4-bbec-e7cf3ca77c63`, lola-stories `019f6a89-ea4a-75d4-90ff-4e809491647e`, marlinjai `019f6a90-8b72-7de9-946f-e81b2ddf3f60`, Opuntia `019f944b-bc25-73c1-be1f-d160c6488694`. Workspaces to move onto the survivor: lola-landing `019ee142-44af-786d-9366-a705b7607f86`, lola-web `019ee142-453c-702c-9e6e-cba872eadcca`, Main `019fa320-8e10-730c-a993-03088d526372`. Magic workspaces still live under lumitra-core: lumitra-studio `019ec2f2-f1a9-7a8a-8ac4-b900384b8b36`, storage-brain `019edcea-d8db-7987-ba82-e588b53c55b7` (delete ONLY after storage-brain S1 confirms nothing resolves through `auth_workspace_id` to the storage-brain one; the SB tenants table maps auth workspace ids).

## Phase 1: admin API completeness + analytics door flip
1. Dispatch `goals/authbrain-admin-api-completeness.md` (auth-brain, `--confirm-stakes`). Ship it. Deploy is automatic on merge; watch it.
2. BEFORE Phase 2 deploys: seed the analytics grant via proxy: `POST /api/admin/machine/app-grants` `{actor_email:"marlinjaipohl@gmail.com", app_slug:"analytics", tenant_id:<lumitra-core>}` (and lola-stories if desired later; not required now).
3. Dispatch `goals/analytics-grant-door.md` (analytics-platform). Ship. Verify live: ungranted probe blocked, granted flow works, tracker/config endpoints unaffected.

## Phase 2: Lumitra org consolidation (pure machine API, agent-first)
Via proxy against the admin machine API, in order: (a) `PATCH workspaces` move the three workspaces above onto lumitra-core; (b) grant `receipts` (and `analytics` if not present) to lumitra-core; (c) delete app-grants held by lumitra-analytics and lumitra (list first, delete each); (d) `DELETE tenants` for lumitra-analytics and lumitra (cascades workspaces/memberships; the machine API is the one built in the entitlements wave); (e) `PATCH tenants` rename lumitra-core to name `Lumitra`, slug `lumitra` (slug freed by the soft-delete); (f) verify: session verify payload shows one Lumitra company with the three workspaces; Studio machine key still 200s `GET https://studio.lumitra.co/api/brands`; receipts door still works; analytics projects still resolve. Studio data stamps and the machine key reference the surviving tenant ID, which never changes.

## Phase 3: storage-brain wave (plan: `docs/plans/2026-07-27-company-isolation.md` in the storage-brain repo, MERGED)
Write goals from that plan's slice definitions (S1 storage-company-keys, S3 storage-hygiene, S4 storage-erasure-consumer as Worker slices; S2 split-tenants is mostly OPERATOR ops with a Worker-built repoint script inside S1 or its own small slice). Registry side (auth-brain, tier 3, covered by this authorization): add the `storage` app entry (`app-grant` kind) + the storage erasure registry entry (url `https://api.storage-brain.lumitra.co/api/internal/erasure` or the path S4 implements, secret name `STORAGE_ERASURE_WEBHOOK_SECRET`); publish any shared/nextjs bumps if types change. Ops between slices: scaffold `STORAGE_ERASURE_WEBHOOK_SECRET` (same value, server-side, auth-brain + storage-brain Infisical projects; find/create the storage-brain Infisical project id via the infra tfstate pattern); seed `storage` grants (lumitra-core survivor, lola-stories, Opuntia when she uploads); create per-company SB tenants via the SB admin API; run the repoint per consumer; mint company-scoped auth-brain keys per consumer app and swap each consumer's `STORAGE_BRAIN_API_KEY` secret server-side (studio, receipts, lola-stories, email-editor, data-table); retire the shared key LAST, after every consumer verifies. Respect the permanent-URL caveat: collect the emitted affected-URL list per consumer and decide regenerate-vs-shim per the plan before revoking anything.

## Phase 4: close out
Update `auth-brain/docs/plans/2026-07-24-authz-hardening.md` gate rows (11 done when storage wave lands), the memory file `authz-decisions-prelaunch-gate.md`, and commit all new goal files to the orchestrator draft PR branch (`worktree-studio-company-isolation-goals`, PR #13). Report per merge. Remaining human-only items to keep surfacing: the Beszel Telegram delivery one-liner (alerts currently deliver to NOWHERE), the lawyer review of erasure/retention, the tenant-level viewer-role product call, and gate items 4 (B-sync + inheritance) and 5+9 (Studio matrix v1 + analytics decision-plane consolidation), which are NOT part of this authorization and need their own tier-3 go.
