---
task: cm-09
spec: docs/plans/2026-06-27-commerce-tenant-db-migration-plan.md
depends_on: [cm-06, cm-08]
verify: pnpm exec tsc --noEmit && pnpm lint && pnpm test && pnpm build
verify_fix_cap: 3
verify_timeout_s: 2400
---

# Goal

Implement spec **CM-09** (section "### CM-09 — Order repo + `createOrder` orchestrator → Kysely (HIGH RISK, isolate)" of `docs/plans/2026-06-27-commerce-tenant-db-migration-plan.md`, plus §5.2 sites #2..#3 + §5.4), using the **EXPAND-CONTRACT (parallel-change) strategy Marlin decided 2026-06-27**. Money/stock-critical.

**Read this framing first, it changes the whole shape of the spec:** the framer-clone verify gate is a WHOLE-PROGRAM typecheck (`tsc --noEmit` + `next build`). The existing `createOrder(prisma, cart)` is called from `src/app/api/commerce/orders/route.ts:183` (owned by CM-10), and `orderRepository` implements the `OrderRepository` interface that other code imports. If you RETYPE `createOrder` / the order repo from Prisma `tx` to Kysely `db` in place, you instantly break the orders route and the verify gate fails. So you do NOT replace in place. You ADD a NEW Kysely order path ALONGSIDE the untouched old Prisma one: a new Kysely order repo and a new Kysely `createOrder` whose transaction calls the NEW Kysely pricing (CM-06) + the NEW INNER Kysely reserve (CM-08), while the old `orderRepository` and old `createOrder(prisma, cart)` stay byte-for-byte intact. The new path is "dark" (no production caller yet). CM-10 flips the orders route to the NEW `createOrder` path; CM-13 deletes the old path and renames the new one to canonical. This matches plan §10 ("the old Prisma path is kept intact through CM-12"). This spec is the EXPAND step for the order repo + createOrder.

THIS IS A HIGH-RISK SPEC. Read `cm-09-recon.md` (the companion in this goals dir) FIRST: it transcribes the exact step order inside the transaction, the seq/insert/find ports, the duplicate/conflict recovery, and the pure tax functions you must keep verbatim. The new path consumes CM-06's NEW Kysely pricing and CM-08's NEW INNER Kysely reserve.

## Read first (the exact semantics + the consumed contracts)

- `cm-09-recon.md` (this directory): the transaction shape (delete BOTH `SET LOCAL` lines on the NEW path: `trx` is already schema-qualified), the `nextOrderNumber` per-tenant `tenantSchemaRef` port, the structured insert/find ports, the step-by-step createOrder body, the `isReserveDuplicate` (name-tag) + `isOrderRequestIdConflict` (pg `23505`) recovery, the pure `computeLineTax`/`resolveLineRate`/`treatmentForRate` functions to keep verbatim, and the DB-enforced accounting identity.
- `cm-08-recon.md` + the NEW reserve funcs CM-08 added: the NEW inner `reserveKysely(trx, ...)` your new createOrder calls (NOT `reserveWithRetryKysely`, so a shortage rolls back the whole order; the inner reserve throws the name-tagged `DuplicateRequestError` out of the tx).
- CM-06's NEW Kysely pricing object (`pricingRepositoryKysely.resolvePrice(db, variantId, opts)`): your new createOrder resolves each line's price through it (a `null` price throws, not a shortage).
- The plan's CM-09 section + §5.2 (site #2 the 2 hand-rolled SET LOCAL deleted on the new path, site #3 the per-tenant `order_number_seq`) + §5.4 (READ COMMITTED, the order-level mirror, server-authoritative integer cents).
- The sources you ADD TO but do NOT rewrite: `src/server/commerce/repository/order.ts` (`orderRepository`: `nextOrderNumber`, `insertOrder`, `insertLineItem`, `findByRequestId`) and `src/server/commerce/order/createOrder.ts` (the whole `createOrder`/`runOrderTransaction`/`resolvePriorOrder`, the pure tax functions, `isReserveDuplicate`, `isOrderRequestIdConflict`, `validateCart`, `Cart`/`CartLine`/`CreateOrderResult`). The old Prisma versions STAY so the orders route keeps compiling.
- The tenant-db raw API + Kysely tx: `node_modules/@marlinjai/tenant-db/dist/*.d.ts` (`tenantDb`, `tenantSchemaRef`), CM-05's `CommerceDB`, CM-02's `src/server/commerce/db.ts` (`commerceTenantDb`), and the Kysely transaction/raw API + postgres.js `23505` error field (same as CM-08: `err.code` + `err.constraint_name`; confirm against `node_modules/postgres` types).

## Definition of done (EXPAND: add new, keep old)

- NEW Kysely order repo added ALONGSIDE the old object in `order.ts` (suggested `orderRepositoryKysely`, methods on the scoped Kysely handle):
  - `nextOrderNumber` -> `sql\`SELECT nextval(${tenantSchemaRef(tgId)} || '.order_number_seq') AS nextval\`` (the per-tenant sequence from CM-04 migration 005), keeping `Number(nextval)` + the `ORD-` 6-zero-pad format. `"order"` stays quoted where raw.
  - `insertOrder`/`insertLineItem` -> `insertInto('order'|'order_line_item').values(...).returningAll().executeTakeFirstOrThrow()` mapping every column from `cm-09-recon.md`.
  - `findByRequestId` -> `selectFrom('order').selectAll().where('request_id','=',requestId).executeTakeFirst()`.
  - Type the new object with a NEW interface co-located IN `order.ts` (do NOT edit `repository/types.ts`: the old `OrderRepository` interface there stays, which is why this spec needs no `commerce-types` shared_state). The new interface uses `CommerceDB`-derived row types, not `@prisma/client` row types.
- NEW Kysely createOrder path added ALONGSIDE the old in `createOrder.ts` (suggested `createOrderKysely(db, cart): Promise<CreateOrderResult>`):
  - Opens `db.transaction().setIsolationLevel('read committed').execute(async (trx) => {...})` with NO `SET LOCAL search_path` (the new `trx` is already schema-qualified). Steps 1..6 preserved in order: (1) sequential idempotency re-read via the new `orderRepositoryKysely.findByRequestId`; (2) `pricingRepositoryKysely.resolvePrice(trx, line.variantId, {...})` per line (a `null` price throws, not a shortage); (3) snapshot variant title/sku/tax_class via a structured Kysely select on `trx` (variant + product.tax_class fallback; do NOT route this through CM-07's read repo, it is an inline tx read) then `computeLineTax`; (4) server-computed integer-cents subtotal/tax/total (cart.clientTotal ignored); (5) new `nextOrderNumber` + `insertOrder`; (6) per-line `insertLineItem` + the NEW INNER `reserveKysely(trx, {... requestId: \`${cart.requestId}:${i}\`, refType:'order_line', refId: lineItem.id})`, throwing `OrderShortageError` on `!result.ok` to roll back the whole order.
  - A NEW `resolvePriorOrderKysely(db, requestId)` re-reads in a FRESH `db.transaction().setIsolationLevel('read committed')` (no `SET LOCAL`), throwing loudly if the prior order is absent.
  - REUSE the pure functions verbatim (`computeLineTax`/`resolveLineRate`/`treatmentForRate`/`validateCart`/`OrderShortageError`/`isReserveDuplicate`): they are DB-free, share them, do NOT duplicate. Add a NEW `isOrderRequestIdConflictPg(err)` that re-detects on `err.code === '23505'` + `err.constraint_name` matching `order_request_id_key` (or containing `request_id`); any other 23505 propagates. (Add it alongside the old Prisma `isOrderRequestIdConflict`; do not retype the old one.)
  - The new outer `createOrderKysely` catch maps `OrderShortageError` -> `{ ok:false, shortages }`, an `isReserveDuplicate(error) || isOrderRequestIdConflictPg(error)` -> `resolvePriorOrderKysely`, else rethrow.
- The OLD `orderRepository`, old `createOrder(prisma, cart)`, old `resolvePriorOrder`, and the `OrderRepository` interface in `types.ts` are LEFT UNTOUCHED so the orders route keeps compiling. (Sanity: `git diff` shows the new repo + new createOrder as ADDITIONS, not signature rewrites; `repository/types.ts` is NOT in the diff.)
- Test `src/server/commerce/order/__tests__/createOrder.isolation.spec.ts` (Testcontainer, TRUST AUTH): a full order placement on the NEW path reserves + writes order + line items atomically (the order row lives in `tg_a`; a `tg_b` handle reading the order id returns ZERO); a shortage on ANY line rolls back the WHOLE order (zero orders, zero reservations); a concurrent duplicate order request_id yields one order and the loser re-reads the SAME orderId (no second order, no double reservation); the persisted row satisfies `total = subtotal + tax_amount` and the DB CHECK rejects a hand-built violating row. PLUS a BACKORDER-at-order-level case (composes with CM-08): a line on a `manage_inventory=true, allow_backorder=true` variant with insufficient stock PLACES the order SUCCESSFULLY (`reserveKysely` returns ok, the line's `inventory_level.available_quantity` goes negative, NO OrderShortageError, the order + line + reservation rows are written), proving a backorder line does not roll back the order. Keep the existing `createOrder.test.ts` unit coverage green WITHOUT porting it (old path untouched).
- `pnpm exec tsc --noEmit && pnpm lint && pnpm test && pnpm build` pass (the orders route still calls the intact old `createOrder`, so nothing is broken). Single conventional commit e.g. `feat(commerce): Kysely order repo + createOrder alongside Prisma (expand), READ COMMITTED + 23505 + total=subtotal+tax (CM-09)`.

## Constraints

- EXPAND ONLY. Add the new Kysely order repo + new createOrder path; do NOT modify the existing `orderRepository`, `createOrder`, the `OrderRepository` interface in `repository/types.ts`, or ANY caller (the orders route, existing tests). Do NOT edit `repository/types.ts` at all (the new interface is co-located in `order.ts`): that is why this spec carries NO `commerce-types` shared_state. If you find yourself editing a call site or `types.ts`, STOP, that is CM-10/CM-13 work.
- Files you may touch: `src/server/commerce/repository/order.ts` (add), `src/server/commerce/order/createOrder.ts` (add), and the createOrder isolation test. Nothing else. Do NOT touch `reserve.ts` (CM-08, consumed read-only via the NEW `reserveKysely`), `read.ts`/barrel (CM-07), `catalog.ts`/`pricing.ts` (CM-06, consumed read-only via the NEW `pricingRepositoryKysely`), the routes (CM-10), the migration set, or Prisma.
- READ COMMITTED is mandatory (b3's guarded-decrement proof) on the new path. The new createOrder MUST call the NEW INNER `reserveKysely(trx, ...)` (not `reserveWithRetryKysely`), so a shortage on any line rolls back the whole order atomically. Money stays server-authoritative integer cents (any client total ignored); the per-line tax math stays integer (Math.round).
- The `order_request_id_key` 23505 re-detection must match the exact constraint name (a miss is a duplicate order, a CRITICAL risk per plan §10). Raw fragments (the seq read) MUST use `tenantSchemaRef(tgId)`; no bare `order_number_seq` / `order` in `sql\`\``.
- Testcontainer tests use the container's TRUST auth (`POSTGRES_HOST_AUTH_METHOD: 'trust'` + username-only `postgresql://role@host` URIs, NO password literals: hardcoded passwords trip GitGuardian and block the PR).
- Do not push to any remote. Output a final completion message: the new function/object names you added, confirmation the old Prisma path + `repository/types.ts` are untouched (so the orders route still compiles), the accounting identity holds on the persisted row, and the shortage-rollback + duplicate-order re-read tests pass against the NEW path.

## Notes

- DAG correction (FLAGGED): the plan §8 lists CM-09 `Deps: CM-07, CM-08`. Under expand-contract the new Kysely `createOrder` calls the NEW Kysely PRICING (CM-06) + the NEW INNER Kysely reserve (CM-08), and reads the variant snapshot inline (it does NOT call the CM-07 read repo). So the correct `depends_on` is `[cm-06, cm-08]`, not `[cm-07, cm-08]`. CM-05's `CommerceDB` is pulled in transitively via CM-06/CM-08. If the operator wants belt-and-suspenders ordering it may add cm-07, but it is not a functional dependency.
- The new funcs carry a temporary `Kysely` suffix so they coexist with the old. CM-13 renames `createOrderKysely` -> `createOrder` (and the order repo) once the old path is deleted, and updates the CM-10 orders-route caller. Leave the suffix in place here.
- The accounting identity (`total = subtotal + tax_amount`) holds because per line `gross == net + tax` in `computeLineTax`, and the order sums net->subtotal, tax->tax_amount, gross->total. Do not "simplify" the totals math; keep the per-line accumulation so the identity is structurally true and the DB CHECK never trips on a real order.
- The `.itest`/isolation test needs Docker/Testcontainers and runs under CI; the in-loop verify is unit + build. The atomic-rollback + duplicate-order proofs are the CI gate.
